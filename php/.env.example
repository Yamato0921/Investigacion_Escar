DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=escar_db
JWT_SECRET=replace_with_a_strong_random_secret
CORS_ORIGIN=
DB_PORT=3306
# Optional: path to unix socket, e.g. /var/run/mysqld/mysqld.sock
DB_SOCKET=
# Optional: path to CA certificate file for TLS connections
DB_SSL_CA=
